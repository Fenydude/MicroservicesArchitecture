spring:
  application:
    name: customer
  datasource:
    password: password
    username: amigoscode
    url: jdbc:postgresql://localhost:8081/customer
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8080